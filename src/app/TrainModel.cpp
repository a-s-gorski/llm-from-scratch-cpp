#include "llm_fs/model/dataset/GPTDataset.h"

using namespace llm_fs::model::dataset;



int main() {
    // auto dataset = GPTDataset();

    // torch::Tensor a = torch::ones({2, 2}, torch::requires_grad());
    // torch::Tensor b = torch::randn({2, 2});
    // auto c = a + b;
    // c.sum().backward();

    return 0;
}